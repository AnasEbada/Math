
# *  f(x) = (x2 + x - 2) / (2x2 - 2x - 3) 

def getvalues():
    '''
    validinputs = ['1','2','3','4','5','6','7','8','9','0','x','+','-','^']
    essentialinputs = ['1','2','3','4','5','6','7','8','9','0','x']

    def is_valid(poly):
        for char in poly:
            if char not in validinputs:
                print('Invalid Input: Contains invalid characters. Please Try Again.')
                return False
    #?  if not any(char in essentialinputs for char in poly):
            print('Invalid Input: Must contain at least one number or variable (x).')
            return False
        return True

    while True:
        px = input('Please Enter P(x): ').replace(" ", "")
        if is_valid(px):
            break

    while True:
        qx = input('Please Enter Q(x): ').replace(" ", "")
        if is_valid(qx):
            break

    return px, qx
    '''

def zeros(px: str, qx: str):
    # 2x^2
    '''
    px.replace("^", "**")
    qx.replace("^", "**")
    #? px.replace("x", "")
    '''
    return

#TODO we are going to study this, but not now. pasued project.
#TODO SymPy

#* New Functions 
#! any()
#! split()
# re library
    #! re.split(r"[]", str)
#! str.replace("new", "old")
#? eval()



    





